'use client';
export function GridRenderer({ viewport, color = 'rgba(255,255,255,.05)', rows = 6, columns = 8 }) { return (ctx) => { ctx.save(); ctx.strokeStyle = color; ctx.lineWidth = 1; for (let row = 1; row < rows; row += 1) { const y = Math.round((viewport.state.height / rows) * row) + .5; ctx.beginPath(); ctx.moveTo(0, y); ctx.lineTo(viewport.state.width, y); ctx.stroke(); } for (let column = 1; column < columns; column += 1) { const x = Math.round((viewport.state.width / columns) * column) + .5; ctx.beginPath(); ctx.moveTo(x, 0); ctx.lineTo(x, viewport.state.height); ctx.stroke(); } ctx.restore(); }; }
