'use client';
import { renderDrawing } from '../drawing/DrawingDefinitions';
export function DrawingRenderer({ drawings, transform, selectedId, selectedIds }) { const selectedSet = selectedIds ? new Set(selectedIds) : null; return (ctx) => { drawings.forEach((drawing) => { const points = drawing.anchorPoints.map(transform.anchorToPixel).filter(Boolean); if (!points.length) return; const [a, b = a] = points; ctx.save(); ctx.strokeStyle = (selectedSet ? selectedSet.has(drawing.id) : drawing.id === selectedId) ? '#4d7cfe' : (drawing.style?.color || '#f5b93e'); ctx.fillStyle = ctx.strokeStyle; ctx.lineWidth = drawing.style?.lineWidth || 1.5; renderDrawing(ctx, drawing, a, b, transform); ctx.restore(); }); }; }
