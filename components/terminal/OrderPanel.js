'use client';
export default function OrderPanel({ open, selection, chain, side, setSide, lots, setLots, sl, setSl, tp, setTp, onClose, onSubmit }) {
  if (!open || !selection) return null;
  return <div style={{ position: 'fixed', inset: 0, zIndex: 100, background: 'rgba(0,0,0,.56)', display: 'grid', placeItems: 'center', padding: 18 }} onClick={(e) => e.target === e.currentTarget && onClose()}><div className="card" style={{ width: 'min(420px,100%)' }}><div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 14 }}><h3>{selection.underlying} {selection.strike} {selection.type}</h3><button className="btn btn-line btn-sm" onClick={onClose}>✕</button></div><p className="muted" style={{ fontSize: 12 }}>EXP {chain?.expiry} · lot {chain?.lot}</p><div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 8, margin: '14px 0' }}>{['BUY', 'SELL'].map((value) => <button key={value} className="btn" onClick={() => setSide(value)} style={{ color: value === 'BUY' ? 'var(--green)' : 'var(--red)', border: '1px solid var(--line2)', background: side === value ? 'rgba(77,124,254,.18)' : 'transparent' }}>{value}</button>)}</div><div className="field"><label>Lots</label><input value={lots} type="number" min="1" onChange={(e) => setLots(e.target.value)} /></div><div className="field"><label>Stop Loss</label><input value={sl} onChange={(e) => setSl(e.target.value)} /></div><div className="field"><label>Target</label><input value={tp} onChange={(e) => setTp(e.target.value)} /></div><button className={'btn ' + (side === 'BUY' ? 'btn-green' : 'btn-red')} style={{ width: '100%' }} onClick={onSubmit}>{side} {lots} lot</button></div></div>;
}
